version: '3.8'

services:
  autohire-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: autohire-ai-capstone
    ports:
      - "3000:80"
    restart: always
    environment:
      - NODE_ENV=production
      - OMNIROUTE_GATEWAY_PORT=8080
